# Random Quote Generator

This is a simple website that displays a random quote from `quoteslate.vercel.app/api/quotes/random`. It shows the quote, the author of the quote and a button to fetch a new quote replacing the existing ones.
